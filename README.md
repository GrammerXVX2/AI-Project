# 🚀 AI Server RAG Project

> Личное рабочее пространство для AI сервера с RAG функциональностью

---

## 📋 Первый запуск на новом компьютере

### ⚠️ Важно!
> В репозитории **НЕТ** папки `Scripts/` и `Lib/` - это виртуальное окружение Python, которое генерируется автоматически. Это правильно! ✅

### 1️⃣ **Создание Virtual Environment**

После клонирования репозитория, создайте виртуальное окружение:

#### Windows (PowerShell)
```powershell
cd path\to\your\project
python -m venv venv
```

#### Windows (Command Prompt)
```cmd
cd path\to\your\project
python -m venv venv
```

#### Linux/Mac
```bash
cd path/to/your/project
python3 -m venv venv
```

> ✅ Это создаст папку `venv/` с `Scripts/` (или `bin/` на Linux) и `Lib/`

---

### 2️⃣ **Активация Virtual Environment**

#### Windows (PowerShell)
```powershell
.\venv\Scripts\Activate.ps1
```

#### Windows (Command Prompt)
```cmd
.\venv\Scripts\activate.bat
```

#### Linux/Mac
```bash
source venv/bin/activate
```

✅ После активации вы должны увидеть `(venv)` в начале строки терминала

---

### 3️⃣ **Установка зависимостей**

```bash
pip install -r requirements.txt
```

> Это установит все необходимые пакеты из списка

---

### 4️⃣ **Деактивация Virtual Environment**

Когда закончите работу:

```bash
deactivate
```

---

## 🔄 Повторный запуск после перезагрузки

Просто активируйте виртуальное окружение (шаг 2️⃣ выше):

```powershell
# Windows PowerShell
.\venv\Scripts\Activate.ps1

# Linux/Mac
source venv/bin/activate
```

Окружение уже будет со всеми установленными пакетами!

---

## 📁 Структура проекта

```
project/
├── venv/                   # 🚫 Не в git - создается локально!
│   ├── Scripts/           # Python окружение (Windows)
│   └── Lib/               # Установленные пакеты
├── ai_server/             # Основной проект
│   ├── main.py            # Главный файл
│   ├── ImGen.py           # Image Generation
│   ├── ingest.py          # Data Ingestion
│   ├── monitor.py         # Monitoring
│   ├── chats/             # Chat data (не в git)
│   ├── models/            # ML Models (не в git)
│   ├── rag_db/            # RAG Database (не в git)
│   ├── static/            # Static files
│   └── RAG data/          # 📊 RAG documents (маленькие данные)
├── client/                # Frontend client
├── requirements.txt       # 📋 Список зависимостей
├── .gitignore            # Что НЕ коммитится
└── README.md             # Этот файл
```

> **Жирным** отмечены файлы в репозитории
> **Курсивом** отмечены локальные файлы (не в git)

---

## 🔧 Полезные команды

| Команда | Описание |
|---------|---------|
| `python main.py` | Запустить основной скрипт |
| `python -m pip list` | Показать все установленные пакеты |
| `pip show package_name` | Информация о пакете |
| `pip install package_name` | Установить новый пакет |
| `pip uninstall package_name` | Удалить пакет |
| `pip freeze > requirements.txt` | Сохранить зависимости |

---

## ⚙️ Проверка окружения

```bash
# Проверить версию Python
python --version

# Проверить, активно ли окружение
pip show pip

# Список всех установленных пакетов
pip list
```

---

## 🚨 Часто встречающиеся проблемы

### ❌ "Scripts is not recognized"

**Проблема:** Команда не найдена при активации

**Решение:** Убедитесь, что:
1. Виртуальное окружение создано (`python -m venv venv`)
2. Вы в правильной папке проекта
3. Используете правильный path:

```powershell
# Windows - должны быть в папке проекта
.\venv\Scripts\Activate.ps1

# Linux/Mac
source venv/bin/activate
```

### ❌ "Permission denied" (PowerShell)

**Решение:** Разрешить запуск скриптов (одноразово):

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### ❌ "ModuleNotFoundError"

**Проблема:** Пакет не установлен

**Решение:** 
1. Проверьте, активно ли окружение (должно быть `(venv)` в начале строки)
2. Установите пакет: `pip install missing_package`
3. Обновите `requirements.txt`: `pip freeze > requirements.txt`

### ❌ "python: command not found"

**Проблема:** Python не установлен

**Решение:** 
- Windows: скачайте с [python.org](https://python.org) и отметьте "Add Python to PATH"
- Linux: `sudo apt install python3`
- Mac: `brew install python3`

---

## 📝 Заметки о Virtual Environment

### ✅ ЧТО В РЕПОЗИТОРИИ:
- `requirements.txt` - список зависимостей
- `.gitignore` - исключает локальные файлы
- `README.md` - этот файл с инструкциями

### ❌ ЧТО НЕ В РЕПОЗИТОРИИ:
- `venv/` папка (Scripts/, Lib/, и т.д.)
- `__pycache__/` - кеш Python
- `chats/`, `models/`, `rag_db/` - большие файлы данных
- `.env` - конфиденциальные данные

### 💡 ПОЧЕМУ ТАК?
- **Размер** - окружение занимает 100+ MB, очень тяжело
- **Портативность** - на каждом компе своё окружение
- **Безопасность** - .env не расшаривается
- **Чистота** - репозиторий содержит только исходники

---

## 🔧 Дополнительные команды

| Команда | Описание |
|---------|---------|
| `python -m venv venv` | Создать новое окружение |
| `python main.py` | Запустить основной скрипт |
| `python -m pip list` | Показать все установленные пакеты |
| `pip show package_name` | Информация о пакете |
| `pip install package_name` | Установить новый пакет |
| `pip uninstall package_name` | Удалить пакет |
| `pip freeze > requirements.txt` | Обновить список зависимостей |

---

## 🎯 Основные файлы проекта

- **main.py** - Точка входа приложения
- **ImGen.py** - Генерация изображений
- **ingest.py** - Загрузка и обработка данных
- **monitor.py** - Мониторинг системы

---

**Удачи в разработке! 🎉**
