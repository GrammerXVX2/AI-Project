# 🚀 AI Server RAG Project

> Личное рабочее пространство для AI сервера с RAG функциональностью

---

## 📋 Быстрый старт

### 1️⃣ **Активация Virtual Environment**

#### Windows (PowerShell):
```powershell
.\Scripts\Activate.ps1
```

#### Windows (Command Prompt):
```cmd
.\Scripts\activate.bat
```

#### Linux/Mac:
```bash
source bin/activate
```

✅ После активации вы должны увидеть `(venv)` в начале строки терминала

---

### 2️⃣ **Установка зависимостей**

```bash
pip install -r requirements.txt
```

Если файла `requirements.txt` нет, создайте его на основе установленных пакетов:
```bash
pip freeze > requirements.txt
```

---

### 3️⃣ **Деактивация Virtual Environment**

Когда закончите работу:
```bash
deactivate
```

---

## 📁 Структура проекта

```
venv/
├── 📂 ai_server/           # Основной проект
│   ├── main.py            # Главный файл
│   ├── ImGen.py           # Image Generation
│   ├── ingest.py          # Data Ingestion
│   ├── monitor.py         # Monitoring
│   ├── 📂 chats/          # Chat data
│   ├── 📂 models/         # ML Models
│   ├── 📂 rag_db/         # RAG Database
│   ├── 📂 static/         # Static files
│   └── 📂 RAG data/       # RAG documents
├── 📂 client/             # Frontend client
├── 📂 Scripts/            # Virtual environment scripts
├── 📂 Lib/                # Virtual environment libraries
└── README.md              # Этот файл
```

---

## 🔧 Полезные команды

| Команда | Описание |
|---------|---------|
| `python main.py` | Запустить основной скрипт |
| `python -m pip list` | Показать все установленные пакеты |
| `pip show package_name` | Информация о пакете |
| `pip install package_name` | Установить новый пакет |
| `pip uninstall package_name` | Удалить пакет |
| `pip freeze > requirements.txt` | Сохранить зависимости |

---

## ⚙️ Проверка окружения

```bash
# Проверить версию Python
python --version

# Проверить текущее окружение
pip show pip

# Список всех пакетов
pip list
```

---

## 🚨 Часто встречающиеся проблемы

### ❌ "Scripts is not recognized"
**Решение:** Убедитесь, что вы в корректной папке и используете правильный path:
```powershell
cd C:\Users\artem\Desktop\Workish\Мусор\venv
.\Scripts\Activate.ps1
```

### ❌ "Permission denied"
**Решение (Windows PowerShell):**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### ❌ "ModuleNotFoundError"
**Решение:** Установите недостающий пакет:
```bash
pip install missing_package
```

---

## 📝 Заметки

- 🔒 Никогда не коммитьте `venv/` папку в git (используйте `.gitignore`)
- 📦 Всегда обновляйте `requirements.txt` при добавлении новых пакетов
- 🐍 Используйте Python 3.8+ для лучшей совместимости
- 💾 Регулярно сохраняйте зависимости: `pip freeze > requirements.txt`

---

## 🎯 Основные файлы проекта

- **main.py** - Точка входа приложения
- **ImGen.py** - Генерация изображений
- **ingest.py** - Загрузка и обработка данных
- **monitor.py** - Мониторинг системы

---

**Удачи в разработке! 🎉**
